import { StyleSheet } from 'react-native'
import { CLR_WHISPER } from '../../common/vars'

export default StyleSheet.create({
  container: {
    height: 8,
    width: 120,
    backgroundColor: CLR_WHISPER,
    borderRadius: 4,
  },
  progress: {
    height: 8,
    backgroundColor: '#19d87c',
    borderRadius: 4,
  },
})
