import { StyleSheet } from 'react-native'
import { TEXT_H2 } from '../../common/typography'

export default StyleSheet.create({
  text: {
    ...TEXT_H2,
    marginHorizontal: 20,
    marginBottom: 16,
  },
})
