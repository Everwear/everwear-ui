import { StyleSheet } from 'react-native'
import { TEXT_H1 } from '../../common/typography'

export default StyleSheet.create({
  text: {
    ...TEXT_H1,
    marginHorizontal: 20,
    marginBottom: 16,
  },
})
